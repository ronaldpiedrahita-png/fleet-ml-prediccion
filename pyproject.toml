[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "fleetml"
version = "0.1.0"
description = "Mantenimiento predictivo de flotas de tracto-camiones"
requires-python = ">=3.12"

[tool.setuptools.packages.find]
where = ["src"]
